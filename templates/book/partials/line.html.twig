    <tr id="l-{{ line.number }}">
        <td class="left-td">
            <a href="#{{ line.number }}" id="{{ line.number }}">
                {{ line.number }}:
            </a>
        </td>
        <td>
            <a href="{{ location.source.url|format(location.externalId, line.time) }}" target="_blank">
                {{ line.text }}
            </a>
        </td>
    </tr>
